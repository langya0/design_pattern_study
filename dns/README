.
├── dns_obs.cpp		//观察者模式模拟dns解析过程
├── dns_res.cpp		//责任链模式模拟dns解析过程
└── recorder.h 		//dns数据包定义 ip domain owner

观察者方式：每一级域名解析器判断该域名是否可以解析完全，如果可以，那么解析并返回;如果不能，那么根据责任链提交给上层。解析完成，最终解析者返回数据

观察者模式：每一级解析器判断是否可以解析域名，如果可以解析，那么解析;如果不能，那么交给上层进行解析，解析完成后，返回。最终显示解析者实际为入口点。
ps每一级解析器既是观察者，又是被观察者。所以notify中执行的依旧是被观察方法（updata）


0 directories, 4 files
